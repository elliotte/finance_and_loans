
text = '<article class="type-system-slab">' + 
		   '<p>' + "Successfully added disclosure, you will need to refresh the page to see it" 
		   + '<a href="<%= report_path(@report) %>">' + ' REFRESH ' + '</a>' + 
		   '</p>' + 
	    '</article>'

$('#bg-screen-for-modal').addClass('open')
$('#bg-screen-for-modal').find('section').empty().append(text) 


